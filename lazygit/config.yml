gui:
  border: single

  screenMode: half

  filterMode: fuzzy

  spinner:
    frames:
      - "⠋"
      - "⠙"
      - "⠹"
      - "⠸"
      - "⠼"
      - "⠴"
      - "⠦"
      - "⠧"
      - "⠇"
      - "⠏"

# git:
#   log:
#     showGraph: never
